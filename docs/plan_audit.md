# 계획서 충실도 감사 (2026-07-15)

대상: `docs/captain_tsubasa_card_manager_claude_code_prompt.md` (마스터 계획서)
코드: `feature/continuous-match-engine` · `js/game/` (연속 엔진) · `viewer.html`
방법: 4개 감사 에이전트가 **코드 직접 확인**(문서 주장 불신) + 헤드리스 실측 + 브라우저.

> **결론 먼저: 충실하지 않다.** 카드 시스템(§12)·좌표/결정론(§3)·형상(§5 일부)·오프사이드 뼈대(§10)·재개(§11)는 실제로 구현되어 있으나, **§4 능력치·§5 포메이션·§9 골키퍼·§13 거츠/특수기술·§15 디버그 오버레이는 미구현**이고, §17 규칙 테스트 9개 중 7개가 부재였다. 이번 세션에 실측 버그 4건을 고쳤다.

---

## A. 계획서 마지막 "검증·수정 프롬프트" 12항목

| # | 항목 | 판정 | 증거 / 비고 |
|---|---|---|---|
| 1 | sim/UI 분리·고정 timestep·시드 RNG | **통과** | `match.js:18` SIM_HZ=15, `js/game/` 에 document/window/Date/timer 참조 0건. 단 **`js/main.js:2` 는 아직 레거시 엔진** — 새 엔진 소비자는 viewer.html·sim 뿐 |
| 2 | 군집/패스 순간이동/GK 배회/프레임 난수 | **통과(수정 후)** | 군집 없음(공 10m 내 평균 3.68명), GK 배회 없음(자기 박스 밖 0/81006틱), 프레임 난수 없음. **패스 순간이동 1건 발견 → 수정**(아래 C-4) |
| 3 | 오프사이드 6요소 + 7케이스 테스트로 증명 | **미달** | 2번째 수비수 정렬(`decide.js:147`)·공보다 앞·상대 진영은 정확. **스로인/골킥/코너 예외 미구현**, **관여 판정이 팀 단위**(C-5) → 계획서 테스트 5번 반증. **7케이스 테스트 0/7** |
| 4 | 지원삼각형·역습대비·first/cover/balance | **부분(개선)** | first defender·cover·역습대비(최후방 2명+GK) 구현됨. **지원 삼각형이 정적 앵커 기반이라 두 서포트가 같은 쪽 67.7% → 수정**(C-3). 디버그 오버레이 없음 |
| 5 | GK 전용 상태·의사결정·손·백패스·1대1 | **미구현** | 계획서 §9 첫 문장이 금지한 `role==='GK'` 분기 방식 그대로. `gk.js` 없음. 선방=`updatePossession` 곁가지(`decide.js:94-113`), **다이빙이 z만 보고 y(높이) 무시**, 박스 손 판정·백패스·1대1·크로스 판단 전부 없음 |
| 6 | 카드가 실제 AI modifier 적용 | **통과** | `resolvedFor()` 가 utility(`decide.js:183,186,229,234`)·포지션(`attack.js`)·압박(`ai.js:33`)·마크(`defend.js`)에서 실제 소비됨. **단 데드 키 3종**(C-6) |
| 7 | 카드가 기본행동 잠금/자동득점 강제 안 함 | **통과** | NEXT_ACTION 은 효용 배수만(잠금 없음), 슛은 카드 무관 `openness` 게이트 통과 필요, `all_out` 도 최후방 2명 유지, 오프사이드 우회 불가 |
| 8 | 시네마틱 결과가 시뮬 선계산·레이더 일치 | **미구현(의도적)** | `DECISION_WINDOW`/`CINEMATIC_RESOLUTION` 페이즈 없음. **유저가 §0.1 에서 "텍스트/중계 중심"으로 정체성 재정의** → 시네마틱 대신 중계 엔진에 투자. 이탈을 정직히 기록 |
| 9 | 같은 시드+카드로그 재현 | **통과** | `match-smoke.mjs:59` (seed42 로그 일치), `cards-lint.mjs:80` , `stability.mjs:56` (카드 스크립트 동반 2회 실행 일치) |
| 10 | 100경기 예외/NaN/deadlock/중복재개 | **통과** | `sim:stability` 100/100 green |
| 11 | 1280×720 카드/필드/점수판 미겹침 | **미달 → 수정** | **감사 중 실제 위반 발견**: 1280×720 에서 손패 top=797 > 720 — **자기 손패를 보려면 스크롤해야 했다**(페이지 936px, 세로 오버플로 216px). 카드 게임으로서 치명적. `.wrap` 을 뷰포트 높이 flex 열로 바꿔 손패를 바닥 고정·중계는 내부 스크롤·필드는 비율 축소 → **1280×720 / 1920×1080 둘 다 오버플로 0 · 손패 6장 완전 노출 · 겹침 0** (스크린샷 확인) |
| 12 | 타입검사·테스트·빌드 | **N/A + 통과** | 빌드/타입 없는 vanilla JS ES module(계획서 §0 이 스택별 적용 허용). 게이트 6종 green |

---

## B. §17 필수 테스트 — 규칙 항목 (감사 시점)

| 요구 테스트 | 상태 |
|---|---|
| 11명 중 1명 GK | ✅ `match-smoke.mjs:29` |
| 후반 방향+포메이션 반전 | ⚠️ 방향만 단언, 포메이션 반전 미단언 |
| 공 전체가 골라인 통과시만 득점 | ❌ 없음 (코드도 **공 중심** 기준 — 공 반지름 상수 부재) |
| 마지막터치팀에 따른 스로인/골킥/코너 | ❌ 없음 (기능은 정확히 구현됨) |
| 박스 안 반칙 → PK | ❌→✅ **이번에 기능+테스트 신설** |
| 옐로2/레드 후 제거 | ❌→✅ **이번에 기능+테스트 신설** |
| GK 박스 밖 손 금지 | ❌ 기능·테스트 모두 부재 |
| 의도적 백패스 손 금지 | ❌ 기능·테스트 모두 부재 |
| 오프사이드 7케이스 | ❌ 0/7 |

## C. 이번 세션에 실제로 고친 것 (계획서: "보고만 하지 말고 고쳐라")

1. **패스 후 선수 동결 — 실측 최악 버그.** `ball.js setFlat` 이 `ownerId` 만 지우고 `carrierId` 를 안 지워, `ai.js` 는 "캐리어는 decide 가 옮긴다"고 건너뛰고 `decide` 는 `CONTROLLED` 일 때만 옮겨서 **비행 내내 아무도 패서를 안 옮김**. 실측 IN_PLAY 틱의 **23.4%**, 경기당 232회, 최장 5.73초 정지. → `setFlat` 에서 `carrierId=null`. **실측 23.4% → 0.0%**
2. **§11 반칙 계층 신설.** 태클 resolver(등 뒤 진입·강도·시드난수 → 합법/careless/reckless/excessive), `FOUL` 독립 이벤트, 직접 프리킥(상대 9.15m 이격), **자기 박스 반칙 → PK**(키커=마크, GK=골라인, 나머지 박스 밖·마크 뒤), 옐로 누적 2장=레드, `SENT_OFF` → 피치 밖 제거. `clampPlayers`/`resetToFormation`/`resetFormation` 이 퇴장 선수를 되살리던 것도 차단(§4 불변조건). **실측 튜닝: 반칙 27 · 옐로 4.4 · 레드 0.05 · PK 0.35 · 퇴장 0.65/경기** (초기값은 PK 48회/경기였음 — 접촉의 59%가 박스에 몰려서. `boxCaution` 항으로 해결)
3. **지원 삼각형.** 두 서포트의 side 를 정적 `homeAnchor.az` 대신 캐리어 기준 실제 위치로 잡고, 겹치면 반대쪽으로 강제. **반대쪽 비율 32.3% → 46.5%** (즉시 100%가 아닌 건 목표만 주고 실제 이동은 운동학·분리력이 지배하기 때문)
4. **GK 파리 시 공 순간이동(§18 금지).** 공을 GK 좌표로 덮어쓰던 것(1틱 9.53m 점프) 제거 — 공은 제자리에 두고 속도만 부여
5. **골든 baseline 재산출.** `cards-lint` 회귀 잠금 `4-5|ev1704` → `4-6|ev1771` (엔진 의도 변경 반영; 테스트 목적은 유지)
6. **1280×720 에서 손패가 폴드 밖(§2·§17).** 손패 top=797 > 720 이라 **자기 패를 보려면 스크롤해야 했다.** `.wrap` 뷰포트 높이 flex 열 + 중계 내부 스크롤 + 필드 비율 축소 → 두 해상도 모두 오버플로 0·손패 완전 노출
7. **허위 카드 문구 제거(§13 미구현 정직화).** `conserve_guts` 는 `gutsThresh` 소비자가 0이라 **CP만 먹고 아무 일도 안 하는 카드**였다 → 덱에서 제외(16→15장, 계획서 최소 12장 충족). `flair` 의 "거츠 소모가 증가한다" → 실제 효과(드리블 편향·턴오버 위험)로 정정. 데드 키 `gutsThresh` 제거
8. **`sim:rules` 게이트 신설(18단언).** 반칙 발생·FoulEvent 스키마·빈도 범위·프리킥/PK·옐로2=레드=퇴장·§4 불변조건·중계 노출. **공허참 방지 단언 포함**(표본에 실제 퇴장이 존재하는지 먼저 확인 — `positioning-lint` 가 길이 2 배열을 ≤2 로 단언하던 동어반복을 반복하지 않기 위해)

## D. 게이트 결과 (이번 세션 종료 시점)

```
sim:match ✅   sim:pos ✅(재현성 포함)   sim:play ✅
sim:seq   ✅ 편차 7.0%p ≤ 8%p (9.5 / 43.5 / 27.1 — 디자이너 목표 10/40/30 유지)
sim:cards ✅ 59/59        sim:stability ✅ 100/100
브라우저: 콘솔 에러 0 · 1280×720/1920×1080 레이아웃 미겹침 · 반칙/프리킥/PK 중계 노출
```

## E. 남은 미구현 (정직하게 — 고치지 못했거나 의도적 이탈)

| 영역 | 상태 | 비고 |
|---|---|---|
| §4 `PlayerAttributes` (22종) | **미구현 (0/22)** | **22명이 역학적으로 완전히 동일.** 결과는 전역 상수(`saveProb`·`turnoverBase`)에서만 나옴. `subBoost` 는 팀 단위 보정이지 선수 능력치 아님. §4 최대 결손이며 §13·§9 의 선행 조건 |
| §4 `stamina`/`guts`/`traits`/`cooldowns`/`facing` | 미구현 | PlayerState 9/19 필드 |
| §5 포메이션 4-4-2 / 3-5-2 | **미구현 (1/3)** | `FORMATION_433` 만. `field.js:49` 주석이 자인. (`data/formations.json` 은 레거시 엔진용) |
| §5 TeamPhase 6종 분류 | 미구현 | 전환 3~5초 개념은 카드 타이밍 게이트로만 존재, AI 행동은 안 바뀜 |
| §5 CB 커버 / DM 보호 | 미구현 | `role==='CB'`/`'DM'` 분기 0건 — 순수 depthRank lerp |
| §5 압박 인원 캡 | **부분** | 압박 2명은 맞으나 **캡 상수가 아니라 1+1 하드코딩의 우연**. `press:'high'` 는 인원을 안 늘림(거리만). `positioning-lint:69` 는 길이 2 배열을 ≤2 로 단언하는 **동어반복 테스트** |
| §6 원투패스·클리어·안전드리블 | 미구현 | 수비 행동은 utility 없음(위치·확률 창발) |
| §6 공중볼 경합/헤더 | 미구현 | 컨트롤이 `ball.y` 를 무시 → 3m 높이 공도 지상처럼 수집 |
| §9 골키퍼 전용 모델 | **미구현** | 계획서가 명시적으로 금지한 방식. 다이빙 y 무시, 손/백패스/1대1/크로스 판단 없음 |
| §10 스로인/골킥/코너 오프사이드 예외 | 미구현 | `exemptRestart` 0건 |
| §10 관여 판정 선수 동일성 | **미구현** | 스냅샷이 `{team,x,z}` 뿐 → 팀 단위 매칭. 계획서 테스트 5 반증 상태 |
| §11 공 "전체" 골라인 통과 | 미달 | 공 중심 기준. 공 반지름 상수 자체가 코드에 없음 |
| §11 킥오프 합법성 | 미구현 | `keepOutRadius`(9.15) 선언만, 참조 0건 — W/ST 가 킥오프 때 상대 진영에 섬 |
| §12 stale swap (비활성 카드 교환) | **미구현** | `staleSwapSec:40`·`drawnAt` 만 있고 swap API 0건 — **데드 설정** |
| §12 맥락 카드 1~2장 | **미달** | 정의된 4장을 전부 주입하고 `handMax` 를 우회 → 손패 10장 가능 |
| §12 CardTiming 3종 | **데드 분기** | `GOALKEEPER_DUEL`/`SET_PIECE` 가 `state.pendingEncounter`/`state.restart` 를 읽는데 **그 필드는 아무도 안 씀**; `STOPPAGE` 는 존재하지 않는 phase |
| §12 `scope` | **무시됨** | `effects.js applyMod` 가 `m.scope` 를 안 읽음 — ROLE/ZONE mod 추가 시 조용히 팀 전역 적용 |
| §13 거츠·특수기술 | **미구현** | `gutsThresh` 키만 있고 소비자 0. **`flair` 카드의 "거츠 소모가 증가한다"·`conserve_guts` 는 뒷받침 로직 없는 허위 문구** |
| §15 디버그 오버레이(D키) | **미구현** | `keydown` 은 Esc 하나뿐. 오프사이드 라인·앵커·역할·궤적 렌더 0. 시드 입력·로그 내보내기·리플레이·10배속(현재 4x 최대) 없음 |
| §8 시네마틱/Encounter | 미구현(의도적) | 유저가 텍스트 중심으로 재정의(§0.1) |

## F. 문서 정합성 — 내 과거 주장의 정정

- 커밋 `52562bf` **"마지막 미연결 키(commitForward·recover) 완결 — 모든 카드가 실제로 경기를 바꿈"** → **거짓.** `transition`(recover/counterPress)·`overlapSide`·`gutsThresh` 는 지금도 **소비자 0**. 해당 카드는 동반된 `pressAggression`/`lineHeight` 부수효과로만 작동한다.
- `docs/card_system_build_plan.md` S3~S7 체크는 **구현된 범위 안에서는** 사실이나, §12 의 stale swap·맥락 1~2장·scope 를 미구현인 채로 "S5 완료"로 표기했다 → 과대 표기.
- `docs/match_engine_v1_plan.md:89` "Stage 5 골키퍼 실용 수준 완료" → **거짓**(§9 미구현).
- `sim/play-lint.mjs:58,66` "오프사이드 전" 주석은 stale.
- **`sim:cards` 59/59 green 이 이 결함들을 하나도 잡지 못했다** — 린트가 스펙이 아니라 구현을 따라 쓰였기 때문. 게이트 green ≠ 계획서 충실.
